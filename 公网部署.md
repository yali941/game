# 通过 GitHub + Render 免费部署

本目录包含 `render.yaml`，预设一个免费 Docker Web Service，不创建数据库或收费磁盘。首次部署后暂不自动跟随代码提交重新部署，避免更新代码时打断棋局。

## 1. 上传到 GitHub

1. 登录 GitHub，创建名为 `yiju-gomoku` 的仓库。可以使用私有仓库，后续仅授权 Render 读取这个仓库。
2. 将本目录里的文件上传到仓库根目录，包括 `public` 文件夹。仓库根目录应直接显示 `Dockerfile`、`server.js`、`package.json` 和 `render.yaml`；不要把压缩包作为唯一文件上传，也不要多套一层 `gomoku` 文件夹。
3. 提交文件。

## 2. 连接 Render

1. 打开 https://dashboard.render.com/ ，登录或注册自己的账号。
2. 在连接 GitHub 的授权页面，仅选择本游戏的仓库。
3. 选择 **New > Blueprint**，选中 `yiju-gomoku` 仓库。
4. 使用根目录的 `render.yaml`。核对预览：只创建一个名为 `yiju-gomoku` 的 Web Service，计算套餐为 **Free**。不要选择升级收费套餐。
5. 点击部署。等状态变为 **Live** 后，打开平台实际分配的 `https://…onrender.com` 地址。

也可以使用 **New > Web Service**：选择仓库，运行环境选择 **Docker**，Dockerfile 路径为 `./Dockerfile`，实例套餐选择 **Free**。Dockerfile 已包含启动命令，无需额外填写。

## 3. 验证公网联机

先在电脑打开分配的网址创建房间，再用关闭 Wi-Fi、启用移动数据的手机打开同一网址，输入房间码。双方交替落子，确认两边同步。这也能确认没有误用原来的 `192.168…` 局域网地址。

## 免费方案的实际限制

Render 免费 Web Service 在连续 15 分钟没有入站流量后可能休眠，再次访问时需要等待唤醒；平台也可能重启免费实例。当前游戏将棋局存放在内存中，休眠、重启或重新部署后房间会丢失。游戏规则没有落子时限，但免费托管不能保证长时间无操作时棋局始终保留。需要长期保留棋局时，应增加持久化存储；免费实例的本地文件不具备持久化能力。

免费套餐另有运行时长、带宽和构建额度，按平台实际说明为准。

## 参考

- Render Blueprint 配置：https://render.com/docs/blueprint-spec
- Render 免费套餐限制：https://render.com/docs/free
- Render Web Service 部署：https://render.com/docs/web-services
